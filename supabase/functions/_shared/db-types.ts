/**
 * Placeholder DB type. Regenerate against the real project once it's created:
 *   supabase gen types typescript --linked > supabase/functions/_shared/db-types.ts
 * (then re-add the `export type Database = ...` wrapper below if the CLI's
 * output format changes). Left loose for now so function code isn't blocked
 * on a live project existing yet.
 */
// deno-lint-ignore no-explicit-any
export type Database = any;
